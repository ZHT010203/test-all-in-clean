## 接口测试的两大核心点
# 01：从前端进行操作
比如post\put\delete请求。
j这个就可能就是单纯的看断言就行了。看看响应企业如何。
# 02：从前端数据渲染
\get\
这个可能就是需要先。就是通过代码先查询这个这一条测试用例，它那个在数据库里面真实数据是怎么样的，然后呢？再通过接口。看一般结果是get吗？然后呢，看它渲染出来相应的这个数据和数据库比对是否用得上。
是否一样？
就是说要同时实现两个功能，第一个查询数据库，第二个。接口get接口的，然后比对。
可以

## 配置层

## 数据库-造数层


## 业务逻辑层
```
├── conftest.py              # 你的 read_test_case fixture 放这里
├── test_login_api.py        # 登录模块接口
├── test_order_api.py        # 订单模块接口
├── test_user_api.py         # 用户模块接口
└── test_checkout_flow.py    # 核心业务流程（串联多个接口）
```
## 日志层

## 公共工具层
封装http请求
封装数据库操作

思考，是否需要封装断言、读取测试用例。


## tool层
