# 一：配置层Conf目录
- 账户密码一个文件
- url一个文件
- 请求体一个文件

# 二：造数层MockData目录

好
# 三：核心驱动层common目录

## 封装
### http请求
### db连接和操作、增删改查方法
### 读取测试用例
这个的话主要是独立资源的读取的是Excel的用立马是吧？ 然后可能就是需要通过一点就是无论用户他写的。 自动头属于哪个？无论字段头呢？属于写的是。比方说写的接口，入参放在后面或者放在前面顺序发生变化都无所谓。 是吧？ 其实我的思考就是说可以把用户用户可以想要确定自己所去哪个字段的那个字段名，给它作为入参写进去。然后呢，这个封装的后面他就会直接读取这个字名，并且封装成一个，并且返回一个相对应的一个字典字符串。 也行，是吧？ 还可以设置。 读取哪个表是吧？ 等等就是根据通用的来嘛，反正就是慢慢微调。 
### 断言
除了校验数据、校验内容、校验是否为空。
其实还有一个最核心的就是说预期结果。 他写什么具体内容？ 是写具体的数值吗？那么这个断言这个封装的时候，他就可能需要怎么样？ 需要通用，所以说这个怎么解决呢？就很很关键。 比方说断言有的用户他写的是具体数值。 一共多用户呢？他写的是什么？他写的是一种。 一种就是正则表达提取试试吧，因为。因为。 断言，因为一般接口返回的就是一个JSON文件嘛，JSON内容JSON，里面还有很多嵌套的是吧？就是可能就是有些问题是吧，所以说也要。 讨论和思考
### 输出报告
01：回血测试用例


# 四：业务逻辑层
## conftest.py层
## 接口业务逻辑层
├── test_login_api.py        # 登录模块接口
├── test_order_api.py        # 订单模块接口
├── test_user_api.py         # 用户模块接口
└── test_checkout_flow.py    # 核心业务流程（串联多个接口）


# 五：输出测试报告

# 六：日志输出层



# 公共工具层
01：自动控制文件大小
02：抓包


# 存放测试用例
## 造数目录
## 接口测试用例目录