# 接口测试使用说明

新项目接入只需两步：**写 Excel 用例 → 写测试代码**。框架代码（common/）一行不改。

---

## 流程一：写 Excel 用例

### 1. 放哪
`Data/Test_case/你的接口名.xlsx`，例如 `删除品牌.xlsx`。

### 2. 列怎么填

| 列名 | 必填 | 说明 |
|------|------|------|
| `ApiPath` | 是 | 接口路径，如 `/brand/create` |
| `接口入参` | 是 | JSON 字符串，如 `{"name":"万和","firstLetter":"W","sort":1,"showStatus":0}` |
| `预期结果` | 是 | 断言条件，写法见下 |
| `测试点` | 否 | 用例名称，日志提示用 |
| `Method` | 否 | HTTP方法，默认 `POST` |
| `提取` | 否 | 链路用例才填，见末尾"链路用例" |

### 3. 预期结果列怎么写（核心）

**6种写法，分号分隔，用 jsonpath 取值：**

| 写法 | 格式 | 例子 |
|------|------|------|
| 等值 | `$.字段=值` | `$.code=200` |
| 不等 | `$.字段!=值` | `$.code!=500` |
| 非空 | `not_empty=$.字段` | `not_empty=$.data.name` |
| 包含 | `contains=$.字段,值` | `contains=$.message,成功` |
| 类型 | `type=$.字段,类型名` | `type=$.code,int` |
| 长度 | `length=$.字段,比较符数字` | `length=$.data.list,>=1` |

**jsonpath 路径写法：**

| 响应结构 | 写法 |
|---------|------|
| `{"code":200}` | `$.code` |
| `{"data":{"name":"万和"}}` | `$.data.name` |
| `{"data":{"list":[{"name":"万和"}]}}` | `$.data.list[0].name` |

**类型名**：`int` `str` `list` `dict` `float` `bool`
**长度比较符**：`>=1` `<=5` `>0` `<10` `==0` `=3`

**组合示例**（分号隔开，想断几条断几条）：
```
$.code=200;$.message=操作成功;not_empty=$.data.name;contains=$.message,成功;type=$.code,int;length=$.data.list,>=1
```

---

## 流程二：写测试代码

### 1. 放哪
`api_test_framework/test_你的接口名.py`，例如 `test_brand_delete.py`。

### 2. 推荐写法（用 executor，4行）

```python
from common.executor import ApiExecutor

def test_brand_delete(admin_client):
    ApiExecutor(admin_client).run("删除品牌.xlsx")
```

**完事。** 读Excel、发请求、断言、并发、异常兜底、回写——executor 全包了。

`admin_client` 从 [conftest.py](api_test_framework/conftest.py) 自动注入，已带 token，**不用管登录**。

### 3. executor 类的调用方式

```python
from common.executor import ApiExecutor

# 基本用法
ApiExecutor(admin_client).run("删除品牌.xlsx")

# 带参数
ApiExecutor(admin_client, max_workers=10).run("品牌分页列表.xlsx", timeout=120)
```

| 方法 | 参数 | 说明 |
|------|------|------|
| `ApiExecutor(http_client, max_workers=5)` | http_client: conftest的admin_client；max_workers: 并发数 | 实例化 |
| `.run(file_name, timeout=60)` | file_name: Excel文件名；timeout: 整体超时秒 | 跑用例并回写报告 |

### 4. 不用 executor 的手动写法（了解即可）

参考 [test_brand_create.py](api_test_framework/test_brand_create.py)，手动调用这几个类：

| 类 | 实例化 | 调用方法 |
|----|--------|---------|
| `ExcelReader` | `ExcelReader()` | `.read(file_path, required_cols)` 读取用例 |
| `HTTPClient` | conftest 的 `admin_client` | `.post(url, json=...)` 发请求 |
| `AssertEngine` | `AssertEngine(result)` | `.assert_result(预期结果, 测试点)` 断言 |
| `ConcurrentRunner` | `ConcurrentRunner(max_workers=5)` | `.run(tasks, func, timeout)` 并发执行 |
| `ResultWriter` | `ResultWriter()` | `.collect(row, actual, status, err)` 收集 → `.write_back(file_name)` 回写 |

**手动写法约 60 行，executor 写法 4 行，效果一样。能用 executor 就用 executor。**

---

## 换新项目改哪几个文件

| 文件 | 改什么 |
|------|--------|
| `Conf/env/dev.py` | URL、账号、数据库配置 |
| `.env` | 密码（不进 git） |
| `Conf/api_path.py` | 接口路径常量 |

**common/ 目录的代码一行都不用改。**

---

## 运行测试

```bash
# 跑单个文件
pytest api_test_framework/test_brand_delete.py

# 跑全部
pytest api_test_framework/

# 指定环境（默认 dev）
$env:APP_ENV="test"; pytest api_test_framework/
```

---

## 看结果

| 产物 | 位置 |
|------|------|
| 测试报告（Excel） | `Data/Test_report/你的接口名_结果.xlsx` |
| 日志文件 | `Logs/test_run.log` |

报告里每条用例有：实际响应、断言结果（PASS/FAIL/ERROR）、错误信息。

---

## 链路用例（进阶，多接口依赖）

场景：先新增品牌拿 `id` → 再用这个 `id` 删除品牌。

**Excel 多填两列：**

| 步骤 | 接口入参 | 提取 |
|------|---------|------|
| 新增品牌 | `{"name":"万和"}` | `brand_id=$.data.id` |
| 删除品牌 | `{"id":${brand_id}}` | （留空） |

- **提取列**：`变量名=jsonpath`，多个用分号，如 `brand_id=$.data.id;brand_name=$.data.name`
- **入参引用**：用 `${变量名}` 占位，executor 自动替换

executor 检测到"提取"列或入参含 `${` 会自动走串行执行，普通用例仍并发。

---

## conftest 提供的 fixture（直接当入参用）

| fixture 名 | 作用域 | 说明 |
|-----------|--------|------|
| `admin_client` | session | HTTPClient，已带 token，直接用 |
| `db_client` | session | DBClient，查数据库用 |
| `read_excel` | function | 读 Excel 用例（用 executor 时不用这个） |
| `admin_login` | session | 原始登录信息（一般不直接用） |

---

## 一句话总结

**Excel 填三列（ApiPath、接口入参、预期结果）→ 测试文件写4行调 executor → 完事。**
