# HTTP请求类
## 需要添加
01：多线程并发请求，提高速度

对应函数方法：
更新请求头：set_headers()
合并请求头：merge_headers()
request() - 统一请求方法
url是get请求：get()
url是post请求：post()
url是put请求：put()
url是delete请求：delete()
关闭session：close()

## 失败重试装饰器
## 更新请求头：set_headers()
## 合并请求头：merge_headers()
## request() - 统一请求方法
## url是get请求：get()
## url是post请求：post()
## url是put请求：put()
## url是delete请求：delete()
## 关闭session：close()





# 数据库类
01：支持增删改查
02：每个方法都能批量操作和单条操作，自己选择。







# 断言类

思考：
01：断言重新封装，excle重新写：用jsonpath表达式最好，能处理巨大多数情景
$.code=200; $.data.user.id=10086; $.message=操作成功
规范一下写的测试用例怎么写呢？

## 01：校验状态码；
## 02：校验数据类型；
## 03：校验数据结构。比如json字段的结构什么的；
## 04：校验数据是否为空；
## 05：校·验数据是否符合预期。

特殊方法：
01：校验响应数据中是否存在指定的字段。响应返回一千条数据当中，你只需要找到你想要的其中一条就行，如何实现？——》生成器还是什么呢？
02：如何判断你想要的值在响应数据中是否存在？——》生成器？？还是什么呢？



# 测试用例类
## 方法：read（）
入参：配置文件路径、表格名称、字段名称，自动返回为数组字典格式。

实现方法流程
01：读取配置文件，校验文件是否存在，表格是否存在。
02：校验必填列是否存在。
03：清洗脏数据：isnull判断是否为空，dropna删除空值。
04：to_dict返回数组字典格式。


# 回写测试用例类

01：输入回写的字段名称还是什么情况？思考一下。


