# 概览
01：通用框架搭建
02：单接口测试实现。
03：多接口链路实现

知道了
慢慢来，不要怕
我知道了

知道了
后 方法
# 需要完成概览
## 断言类
01：断言重新封装，excle重新写：用jsonpath表达式最好，能处理巨大多数情景
$.code=200; $.data.user.id=10086;  $.message=操作成功
02：josn响应数据本身还有那些情景？

## HTTP请求类
01：多线程并发请求，提高速度
02：需要思考一个问题，我现在换了一个测试项目，把这个通用框架拉取下来，我拿到一个接口的url和请求体，我现在只


## 断言重新封装
### 断言重新封装：
01：校验状态码，02：校验数据类型，03：校验数据结构，比如json字段的机构什么的03：校验数据是否为空，04：校验数据是否符合预期。
### 特殊情景：
假设。响应的数据由。几千条。你必须写一个函数用来解决你。这样一个情况。你只这一千条当中，你只需要。找到。你想要的其中一条就行。而你如何判断这一条呢？就是这一条要当中要有你预期的一个数据，所以说我建议用生成器。你应该用生成器能够解决吧？主要是能够解决这样的情况。范湖的数据很多，但是你只需要取其中一条就行了。
但是呢？你并不知道它的索引在哪里，你也不知道它的位置在哪里。你就知道这里面数据在JSON里面。所以说要解决这个情景你建议使用什么？
1:你如何判断你的json里面有你想要的数据。
                  2：假设，现在的响应的数据有很多，你如何实现找到你想要的数据后，直接a把这个对应下标的json内容提取出来，后面的就不用再进行读取了。
                    3:如何判断响应的数据中存在字段？也就是校验响应数据结构、响应数据类型，校验数据是否为空。
# 已经完成
