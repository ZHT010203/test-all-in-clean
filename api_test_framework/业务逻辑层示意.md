# 业务逻辑层
## conftest层。
01：登录tokne传递。
02：实例化HTTP请求类。
03：实例化数据库类。
04：实例化断言类。
05：实例化测试用例类。


## 02：接口测试层
01：后面就是每个接口的一个测试文件，因为每一个接口需要读取测试用例、断言。
02：还可能需要一些前置接口的状态处理，所以一个模块，或者接口一个单独的文件测试。
03：还有一些特殊的入参字段，通过函数构造，动态生成更加方便，比如边界值测试当中，有一个字段需要入参10张图片，这个时候用代码调用更方便些。或者某个字段需要入参1000字的字符串，这个时候用代码调用更方便些。


